# DEV ONLY: These rules use anonymous auth with allow-all authorization.
# Do NOT use in production — any request to these ports gets full admin API access.
- id: kratos-admin
  upstream:
    preserveHost: true
    url: http://kratos:4434
  match:
    url: http://localhost:<1?>4455</?.*>
    methods:
      - GET
      - POST
      - PUT
      - DELETE
      - PATCH
      - OPTIONS
  authenticators:
    - handler: anonymous
  authorizer:
    handler: allow
  mutators:
    - handler: noop
- id: oathkeeper-admin
  upstream:
    preserveHost: true
    url: http://oathkeeper:4456
  match:
    url: http://localhost:<2?>4455</?.*>
    methods:
      - GET
      - POST
      - PUT
      - DELETE
      - PATCH
      - OPTIONS
  authenticators:
    - handler: anonymous
  authorizer:
    handler: allow
  mutators:
    - handler: noop
